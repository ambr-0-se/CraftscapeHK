steps:
  # Build the backend container image
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-t'
      - 'gcr.io/$PROJECT_ID/craftscape-backend:latest'
      - '-f'
      - 'server/Dockerfile'
      - './server'
    id: 'build-backend-image'

  # Get backend URL for frontend build
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        # Try to get existing backend URL, fallback to default
        BACKEND_URL=$$(gcloud run services describe craftscape-backend --region us-central1 --format="value(status.url)" 2>/dev/null || echo "https://craftscape-backend-998275462099.us-central1.run.app")
        echo "$$BACKEND_URL" > /workspace/backend_url.txt
        echo "Backend URL for frontend: $$BACKEND_URL"
    id: 'get-backend-url'

  # Build the frontend container image with backend URL
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '--build-arg'
      - 'VITE_API_BASE_URL=$$(cat /workspace/backend_url.txt)'
      - '-t'
      - 'gcr.io/$PROJECT_ID/craftscape-frontend:latest'
      - '-f'
      - 'Dockerfile'
      - '.'
    id: 'build-frontend-image'
    waitFor: ['get-backend-url']

  # Push the backend container image to Container Registry
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - 'gcr.io/$PROJECT_ID/craftscape-backend:latest'
    id: 'push-backend-image'
    waitFor: ['build-backend-image']

  # Push the frontend container image to Container Registry
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - 'gcr.io/$PROJECT_ID/craftscape-frontend:latest'
    id: 'push-frontend-image'
    waitFor: ['build-frontend-image']

  # Deploy backend container to Cloud Run
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - 'craftscape-backend'
      - '--image'
      - 'gcr.io/$PROJECT_ID/craftscape-backend:latest'
      - '--region'
      - 'us-central1'
      - '--platform'
      - 'managed'
      - '--allow-unauthenticated'
      - '--set-env-vars'
      - 'GEMINI_API_KEY=${_GEMINI_API_KEY}'
      - '--memory'
      - '512Mi'
      - '--cpu'
      - '1'
      - '--timeout'
      - '300s'
      - '--max-instances'
      - '10'
    id: 'deploy-backend'
    waitFor: ['push-backend-image']

  # Deploy frontend container to Cloud Run
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - 'craftscape-frontend'
      - '--image'
      - 'gcr.io/$PROJECT_ID/craftscape-frontend:latest'
      - '--region'
      - 'us-central1'
      - '--platform'
      - 'managed'
      - '--allow-unauthenticated'
      - '--memory'
      - '256Mi'
      - '--cpu'
      - '1'
      - '--timeout'
      - '60s'
      - '--max-instances'
      - '10'
    id: 'deploy-frontend'
    waitFor: ['push-frontend-image']

  # Wait for backend deployment to be ready and seed the database
  - name: 'gcr.io/cloud-builders/gcloud'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        echo "Waiting for backend service to be ready..."
        sleep 10
        
        echo "Getting backend service URL..."
        BACKEND_URL=$$(gcloud run services describe craftscape-backend --region us-central1 --format="value(status.url)")
        echo "Backend URL: $$BACKEND_URL"
        
        echo "Seeding database..."
        curl -X POST "$$BACKEND_URL/admin/seed" \
          -H "Content-Type: application/json" \
          --fail \
          --show-error \
          --retry 3 \
          --retry-delay 5 \
          -v
        
        echo "Database seeding completed!"
    id: 'wait-and-seed'

images:
  - 'gcr.io/$PROJECT_ID/craftscape-backend:latest'
  - 'gcr.io/$PROJECT_ID/craftscape-frontend:latest'

options:
  machineType: 'E2_HIGHCPU_8'
  logging: CLOUD_LOGGING_ONLY

timeout: '1200s'
